# Copy this file to .env and fill in your real MySQL credentials.
# .env itself is gitignored so your real credentials never get committed.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gateman
DB_USER=root
DB_PASSWORD=

# GSM panic-alert module (SIM800L). Leave blank/commented to disable.
# GSM_PORT=COM5
